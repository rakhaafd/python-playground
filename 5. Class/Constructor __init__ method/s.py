class Cat:
    def __init__ (self, name, color):
        self.name = name
        self.color = color

nobi = Cat('nobi', 'black')
nero = Cat('nero', 'white')

print(nobi.name)
print(nobi.color)
print(nero.name)
print(nero.color)